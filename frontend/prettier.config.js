//  @ts-check

/** @type {import('prettier').Config} */
const config = {
  semi: false,
  singleQuote: true,
  trailingComma: 'all',
  tabWidth: 2,
  printWidth: 120,
  arrowParens: 'always',
  bracketSpacing: true,
  endOfLine: 'auto',
}

export default config
